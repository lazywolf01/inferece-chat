services:
  app:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      PORT: 8080
      DATABASE_PATH: /app/data/app.sqlite
      INGESTION_URL: http://localhost:8080/api/ingest/logs
    volumes:
      - app-data:/app/data

volumes:
  app-data:
